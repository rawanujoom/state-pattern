enum ProductStateEnum {
    DRAFT = 'Draft',
    DELETED_DRAFT = 'Deleted Draft',
    AVAILABLE = 'Available',
    RESERVED = 'Reserved',
    DELETED = 'Deleted',
    EXPIRED = 'Expired',
    SOLD = 'Sold',
    RETURNED = 'Returned',
}

export default ProductStateEnum;
